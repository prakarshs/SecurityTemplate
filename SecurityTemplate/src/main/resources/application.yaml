server:
  port: 8080

spring:
  application:
    name: SecurityTemplate
  datasource:
    url: jdbc:mysql://localhost:3306/secudb
    username: root
    password: root
    driver-class-name:
      com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
